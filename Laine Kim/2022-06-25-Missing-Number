class Solution {
    public int missingNumber(int[] nums) {
        int eachVal;
        int missingVal = 0;

        for (int i = 0; i < nums.length; i++) {
            eachVal = i + 1;
            missingVal += eachVal - nums[i];
        }

        return missingVal;
    }
}